import 'package:flutter/material.dart';

class WalletScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Welcome to Wallet Screen", style: TextStyle(fontSize: 20)),
    );
  }
}
