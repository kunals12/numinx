import 'package:flutter/material.dart';

class InvestScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Welcome to Invest Screen", style: TextStyle(fontSize: 20)),
    );
  }
}
