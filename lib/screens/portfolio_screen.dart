import 'package:flutter/material.dart';

class PortfolioScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Welcome to Portfolio Screen", style: TextStyle(fontSize: 20)),
    );
  }
}
